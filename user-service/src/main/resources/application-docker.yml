# Docker 环境配置（MySQL）
server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://mysql-user:3306/user_db?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&characterEncoding=utf8mb4
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
        enabled: true
